/* eslint-disable prettier/prettier */

// Basado en el ENUM de la tabla campos_formulario
export enum TipoHtml {
  TEXT = 'text',
  NUMBER = 'number',
  DATE = 'date',
  SELECT = 'select',
  CHECKBOX = 'checkbox',
  TEXTAREA = 'textarea',
}
